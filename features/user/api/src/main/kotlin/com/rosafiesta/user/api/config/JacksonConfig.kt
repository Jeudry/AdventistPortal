package com.rosafiesta.user.api.config

import org.springframework.context.annotation.Configuration

@Configuration("userApiJacksonConfig")
class UserApiJacksonConfig {
    // La configuración principal está en el módulo app.
    // Esta clase existe solo para evitar conflictos de nombres y permitir que el módulo compile.
}